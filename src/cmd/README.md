This folder should contain all your "main" packages. A folder should be created
for each application that acts as the client to your src packages. Ideally there
should be a single main.go file that only contains enough bootstrap code to load
and start the application. It should be minimal.
